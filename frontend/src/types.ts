export interface ProjectCardProps {
    title: string;
    description: string;
    link?: string;
}

export interface Project {
    id: number;
    title: string;
    description: string;
    link?: string;
}